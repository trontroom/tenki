# 天気予報アプリ 要件定義（簡易版）

## 概要
- 課題: 262083_26sp「OpenCodeでプログラミング」第9〜11回 天気予報提出
- 期限: 2026年7月5日(日) 23:59（Moodle提出）
- データ取得元: Open-Meteo API（無料・非商用利用）

## 成果物
1. 天気予報ページ（GitHub Pagesで公開）
2. GitHubリポジトリ（新規作成するGitHubアカウント、リポジトリ名: `tenki`）
3. 24時間天気の別ページ（加点項目）

## ファイル構成（授業の命名規則に合わせる）
- `tenki_final.html` … メインページ（構造）
- `tenki_final.css` … スタイル
- `tenki_final.js` … ロジック（API取得・描画・localStorage）
- `tenki_hourly.html` / `tenki_hourly.js` … 24時間天気ページ

## 機能要件

### 基本要件（10点）
- 今日・明日・明後日の3日分の天気予報を取得・表示（Open-Meteo daily API, forecast_days=3）
- 現在時刻を1秒ごとに更新表示
- 時・分・秒が1桁のとき先頭に0を付けて2桁表示（例: 09:05:03）
- 地名を検索してlocalStorageに保存し、複数地点を切り替えて表示（Open-Meteo Geocoding APIで地名→緯度経度）
- コンソールにエラーを出さない（fetch失敗時もtry/catchで処理）

### 加点要件（全項目を実装。最大10点＋アピール狙い）
- 地上10mの最大風速（windspeed_10m_max）を降水量の下に表示 (+5)
- 降水確率（precipitation_probability_max）を表示 (+5)
- 日付列に曜日を表示（今日（月）／明日（火）／明後日（水）） (+5)
- 「現在地の天気を表示」ボタン（Geolocation API） (+10)
- 別ページで24時間・1時間ごとの天気/気温/降水量を表示 (+10)

## 公開・提出方法
- 時間の都合上、新規GitHubアカウントは作成せず、既存の `trontroom` アカウント（gh CLI認証済み）を使用
- ローカルで `git init` → commit → `gh repo create` で `tenki` リポジトリ作成 → push → GitHub Pages有効化
- 公開URL: `https://trontroom.github.io/tenki/tenki_final.html`
- 大学サーバー(class.tama.net)へもSSHでデプロイ（授業資料の手順に従う）
- Moodleに「天気予報ページURL」「GitHubリポジトリURL」「アピールポイント」を記載して提出

## 補足・前提
- 大学サーバー(class.tama.net)へのSSHデプロイも授業内で紹介されているが、今回はGitHub Pagesを優先し、必要なら後で追加する
- 現在地機能は逆ジオコーディングを行わず「現在地」という表示名で座標のみ保存する（外部API依存を減らしエラーを防ぐため）
- コード内の関数名・変数名は可読性重視で命名し、授業のコードと完全一致させることは必須としない
